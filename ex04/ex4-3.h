#include <stdio.h>

// 構造体の宣言と関数のプロトタイプ宣言
struct Student {
    int english;
    int math;
    int japanese;
};

double getPow(double, double);
double getAverage(struct Student);
double getDisp(struct Student);
void ex4_1();
void ex4_2();
